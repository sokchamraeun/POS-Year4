# Production API URL (Render backend)
# Override in Vercel dashboard if different
VITE_API_URL=https://pos-year4-backend.onrender.com/api

# Public site URL used to build table QR codes. Must be the real domain so a
# customer's phone can open it from anywhere (not the LAN IP, which only works
# on the same Wi-Fi). Loaded only for production builds.
VITE_FRONTEND_URL=https://pos-year4.vercel.app

# Pusher (WebSocket)
VITE_PUSHER_APP_KEY=6d21975b9ba47d9edfe3
VITE_PUSHER_APP_CLUSTER=ap1

# Production storage URL (Cloudinary or Render storage)
VITE_STORAGE_URL=https://pos-year4-backend.onrender.com/storage
